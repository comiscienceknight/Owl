<UserControl
    x:Class="Owl.ChatHistoryUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Owl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    DataContext="{Binding Chats, Source={StaticResource Locator}}"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <DataTemplate x:Key="ImageTextListCollectionTemplate">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <Border Height="72" Width="81" Margin="5,2,0,15" Grid.Column="0">
                    <Image Source="{Binding Path=UserProfile}" Stretch="UniformToFill"/>
                </Border>
                <StackPanel Orientation="Vertical" VerticalAlignment="Top" Margin="15,4,5,10"
                             Grid.Column="1">
                    <TextBlock Text="{Binding Path=UserName}"  FontSize="30" 
                       Margin="0,0,3,8" TextTrimming="WordEllipsis" FontFamily="Global User Interface" />
                    <TextBlock Text="{Binding Path=Message}"  HorizontalAlignment="Left"
                               Opacity="0.7" Margin="0,2,3,3"
                                FontSize="20" FontFamily="Global User Interface"/>
                </StackPanel>
                <TextBlock Text="{Binding Path=Time}" Grid.Column="2" FontSize="20" Opacity="0.8"
                       Margin="0,3,5,10" VerticalAlignment="Center" FontFamily="Global User Interface" />
            </Grid>
        </DataTemplate>

        <local:ItemClickedConverter x:Name="ItemClickedConverter" />
    </UserControl.Resources>
    <Grid>
        <ListView x:Name="ListView_ChatEntries"  HorizontalAlignment="Stretch"
                  ItemsSource="{Binding Path=ChatEntrytCollection}"
                  VerticalAlignment="Stretch" IsItemClickEnabled="True"
                  ItemTemplate="{StaticResource ImageTextListCollectionTemplate}">
            <i:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="ItemClick">
                    <core:InvokeCommandAction Command="{Binding ItemSelectedCommand}"
                                              InputConverter="{StaticResource ItemClickedConverter}"/>
                </core:EventTriggerBehavior>
                <core:EventTriggerBehavior EventName="Loaded">
                    <core:InvokeCommandAction Command="{Binding ListViewChatEntriesLoadedCommand}"/>
                </core:EventTriggerBehavior>
            </i:Interaction.Behaviors>
        </ListView>

        <ProgressBar x:Name="ProgressBar_Loading" IsIndeterminate="False"
                     HorizontalAlignment="Stretch"        Visibility="Collapsed"
                     VerticalAlignment="Center"/>
    </Grid>
</UserControl>

